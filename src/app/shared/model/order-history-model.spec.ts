import { OrderHistoryModel } from './order-history-model';

describe('OrderHistoryModel', () => {
  it('should create an instance', () => {
    expect(new OrderHistoryModel()).toBeTruthy();
  });
});
