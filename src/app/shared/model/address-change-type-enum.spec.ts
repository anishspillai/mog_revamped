import { AddressChangeTypeEnum } from './address-change-type-enum';

describe('AddressChangeTypeEnum', () => {
  it('should create an instance', () => {
    expect(new AddressChangeTypeEnum()).toBeTruthy();
  });
});
