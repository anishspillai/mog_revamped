import { UserContactDetails } from './user-contact-details';

describe('UserContactDetails', () => {
  it('should create an instance', () => {
    expect(new UserContactDetails()).toBeTruthy();
  });
});
